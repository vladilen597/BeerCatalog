const initialState = {
  isFetching: false,
  beers: [],
};

const displayBeersReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default displayBeersReducer;
